# DevPilot 路线图

## 阶段 0：项目启动

状态：已完成

- 初始化 Vue 3 + Vite + TypeScript。
- 集成 Tauri 2。
- 增加 Windows Tauri 构建环境包装脚本。
- 创建基础项目结构。
- 增加英文和中文 README。

## 阶段 1：应用基础能力

状态：计划中

- 定义主应用布局。
- 在 `src/router` 下增加路由配置。
- 在 `src/stores` 下建立状态管理约定。
- 在 `src/services` 下建立前端服务层约定。
- 在 `src/types` 下增加共享类型定义。
- 在 `src/styles` 下增加全局样式入口。
- 在 `src-tauri/src/commands` 下定义 Tauri 命令组织方式。

## 阶段 2：核心桌面外壳体验

状态：计划中

- 构建第一个可用的桌面应用外壳。
- 增加导航和布局结构。
- 增加设置页面。
- 增加应用元信息展示。
- 增加 Vue 调用 Tauri 原生命令的基础示例。
- 增加前端错误状态和加载状态。

## 阶段 3：开发体验

状态：计划中

- 在 `docs/guides` 下增加开发指南。
- 在 `docs/architecture` 下增加架构说明。
- 增加 lint 和 format 配置。
- 增加测试策略。
- 在 `.github` 下增加 CI 工作流。

## 阶段 4：打包与发布

状态：计划中

- 验证完整 `npm run tauri:build` 安装包打包流程。
- 编写发布流程文档。
- 建立 changelog 维护规则。
- 准备应用图标和元信息。
- 评估签名和更新策略。

## 待办池

- 自动更新支持。
- 本地持久化配置。
- 本地数据库或文件存储层。
- 国际化。
- 主题切换。
- 窗口状态持久化。
